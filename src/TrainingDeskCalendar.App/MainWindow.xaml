<Window x:Class="TrainingDeskCalendar.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="训练桌历"
        Width="1120"
        Height="470"
        MinWidth="840"
        MinHeight="360"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">
  <Window.Resources>
    <Style x:Key="ReadCardStyle" TargetType="Grid">
      <Setter Property="Visibility" Value="Visible" />
      <Style.Triggers>
        <DataTrigger Binding="{Binding IsEditing}" Value="True">
          <Setter Property="Visibility" Value="Collapsed" />
        </DataTrigger>
      </Style.Triggers>
    </Style>
  </Window.Resources>
  <Border x:Name="SurfaceBorder" Margin="10" Padding="14" CornerRadius="8" Background="#D9F7F8FA" BorderBrush="#BFFFFFFF" BorderThickness="1">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <DockPanel Margin="0,0,0,8" MouseLeftButtonDown="OnHeaderMouseLeftButtonDown">
        <StackPanel Orientation="Horizontal">
          <TextBlock FontSize="18" FontWeight="SemiBold" Text="训练桌历" />
          <TextBlock Margin="10,3,0,0" Foreground="#5D6972" Text="两周训练计划" />
        </StackPanel>
        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
          <Button Margin="4,0,0,0" Padding="9,3" ToolTip="上一页" Click="OnPreviousClick" Content="‹" />
          <Button Margin="4,0,0,0" Padding="9,3" ToolTip="回到今天" Click="OnTodayClick" Content="今天" />
          <Button Margin="4,0,0,0" Padding="9,3" ToolTip="下一页" Click="OnNextClick" Content="›" />
          <Button Margin="10,0,0,0" Padding="9,3" ToolTip="打开设置" Click="OnSettingsClick" Content="设置" />
        </StackPanel>
      </DockPanel>
      <TextBlock Grid.Row="1" Margin="0,0,0,8" Foreground="#5D6972" Text="{Binding RangeLabel}" />
      <ItemsControl Grid.Row="2" ItemsSource="{Binding Days}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <UniformGrid Columns="7" Rows="2" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Margin="3" Padding="8" CornerRadius="5" BorderBrush="#28000000" BorderThickness="1" Background="#FFD5DADF" MouseLeftButtonUp="OnCardMouseLeftButtonUp">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <DockPanel>
                  <TextBlock FontSize="11" Foreground="#5F6B74" Text="{Binding Weekday}" />
                  <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <TextBlock Margin="0,0,5,0" FontSize="16" FontWeight="Bold" Text="{Binding DayNumber}" />
                    <CheckBox VerticalAlignment="Center" IsChecked="{Binding IsCompleted}" ToolTip="切换完成状态" Click="OnCompletedClick" />
                  </StackPanel>
                </DockPanel>
                <Grid Grid.Row="1">
                  <Grid Style="{StaticResource ReadCardStyle}">
                    <TextBlock Margin="0,8,0,0" FontSize="12" TextWrapping="Wrap" Text="{Binding DisplayText}" />
                  </Grid>
                  <Border Padding="4" Background="#F2FFFFFF">
                    <Border.Style>
                      <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding IsEditing}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </Border.Style>
                    <StackPanel>
                      <TextBox MinHeight="42" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                      <StackPanel Margin="0,5,0,0" Orientation="Horizontal">
                        <Button Width="22" Height="22" Margin="0,0,4,0" Background="#BFE3DA" Tag="1" ToolTip="青绿色" Click="OnColorClick" />
                        <Button Width="22" Height="22" Margin="0,0,4,0" Background="#C7D8F2" Tag="2" ToolTip="蓝色" Click="OnColorClick" />
                        <Button Width="22" Height="22" Margin="0,0,4,0" Background="#F4D1A6" Tag="3" ToolTip="橙色" Click="OnColorClick" />
                        <Button Width="22" Height="22" Margin="0,0,4,0" Background="#F1C2C2" Tag="4" ToolTip="红色" Click="OnColorClick" />
                        <Button Width="22" Height="22" Margin="0,0,4,0" Background="#D9C7E8" Tag="5" ToolTip="紫色" Click="OnColorClick" />
                        <Button Width="22" Height="22" Background="#D5DADF" Tag="6" ToolTip="灰色" Click="OnColorClick" />
                        <Button Margin="8,0,0,0" Padding="6,2" Content="保存" Click="OnSaveCardClick" />
                      </StackPanel>
                      <TextBlock Margin="0,4,0,0" Foreground="#B42318" FontSize="10" TextWrapping="Wrap" Text="{Binding SaveError}" />
                    </StackPanel>
                  </Border>
                </Grid>
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
      <TextBlock x:Name="DesktopStatusText" Grid.Row="3" Margin="0,8,0,0" HorizontalAlignment="Right" FontSize="11" Foreground="#5D6972" />
    </Grid>
  </Border>
</Window>
